# RES-PARTIAL-001-D — 거래처 미지급금 드릴다운(인라인) 추가

| 필드 | 값 |
|------|-----|
| **상태** | 완료 |
| **완료일** | 2026-05-06 |
| **차단 사유** | 해당 없음 |

## 작업 목적

`resturant_os` 돈관리 화면2(거래처 미지급금)에서 거래처별 집계 목록만으로는 “무엇을 언제 지급해야 하는지”를 바로 확인하기 어렵다. 거래처를 클릭하면 **해당 거래처의 지급 예정 내역을 인라인으로 펼쳐 보여주는 드릴다운**을 추가한다.

## 관련 tasks.md ID

- RES-PARTIAL-001-D

## 수정 파일 목록

- `resturant_os/src/components/money/MoneyClient.tsx`
- `docs/tasks.md`
- `docs/worklogs/2026-05-06_res-partial-001d_supplier-drilldown.md`

## 변경 내용 요약

- “거래처 미지급금” 섹션에서 거래처 row를 클릭하면 선택 상태를 토글하고,
  선택된 거래처의 지급 예정 내역(`active` 중 `counterparty_name` 일치)을 인라인으로 표시한다.
- 라우팅 추가 없이, 단일 컴포넌트 상태로 구현했다.

## migration 여부

- 없음

## 테스트 결과

- linter: `resturant_os/src/components/money/MoneyClient.tsx` 진단 결과 오류 없음

## 남은 위험

- 드릴다운 내역은 화면1의 날짜 필터(3일/이번주/이번달)와 독립적으로 “전체 지급 예정(active)”에서 필터링한다.
  (화면2는 거래처별 전체 미지급 스냅샷을 보여주는 목적이므로 분리된 기준을 채택)
- `counterparty_name`이 동일한 거래처명이 여러 개(중복)일 경우, 집계/드릴다운이 합쳐져 보일 수 있다(현 설계의 의도).

## 다음 권장 작업

- `RES-PARTIAL-001-E`(문구 정렬)로 넘어가거나,
  드릴다운 항목에 “지급 완료” 버튼/상태 표시 등 실행 액션을 붙일지(범위 확정) 결정한다.

